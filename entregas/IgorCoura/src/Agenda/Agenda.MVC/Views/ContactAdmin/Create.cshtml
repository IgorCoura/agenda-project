@using Agenda.MVC.ViewModel

@model BaseAdminContactViewModel<EditContactViewModel>

@{
    ViewData["Title"] = "Contatos";
}


<div>
    @using (Html.BeginForm("Create", "ContactAdmin", FormMethod.Post))
    {
        @await Html.PartialAsync("_FormsContact", @Model.Contact)
    }
    <div>
       <vc:Summary></vc:Summary>
    </div>
</div>

@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    
}

