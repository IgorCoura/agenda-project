@using Agenda.MVC.ViewModel

@model BaseAdminContactViewModel<EditContactViewModel>

@{
    ViewData["Title"] = "Contatos";
}


<div>
    @using (Html.BeginForm("Edit", "ContactAdmin", FormMethod.Post))
    {
        @await Html.PartialAsync("_FormsContact", Model)
    }
    <div>
       <vc:Summary></vc:Summary>
    </div>
</div>

@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    
}

